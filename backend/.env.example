# Server
PORT=8080
NODE_ENV=development
ALLOWED_ORIGIN=http://localhost:1234

# Swiggy proxy defaults (optional overrides)
SWIGGY_DEFAULT_LAT=
SWIGGY_DEFAULT_LNG=
CACHE_TTL=300

# Database (to be added in MongoDB setup step)
MONGODB_URI=
